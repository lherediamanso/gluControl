<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.GluControl" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.GluControl" parent="Base.Theme.GluControl" />

    <style name="Theme.GluControl.Fullscreen" parent="Theme.GluControl">
        <item name="android:actionBarStyle">@style/Widget.Theme.GluControl.ActionBar.Fullscreen
        </item>
        <item name="android:windowActionBarOverlay">true</item>
        <item name="android:windowBackground">@null</item>
    </style>

    <style name="ThemeOverlay.GluControl.FullscreenContainer" parent="">
        <item name="fullscreenBackgroundColor">@color/primary_A100</item>
        <item name="fullscreenTextColor">@color/primary_A100</item>
    </style>
</resources>